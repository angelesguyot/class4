# To run this code, be sure your current working directory
# is the same as where this file is located and then run:
# ruby 5-loops.rb

# Infinite Tacos -> Finite Tacos
# loop do
#   puts "tacos!"
#   if index == 5
#    break
# end

# Loop through tacos
 tacos = ["carnitas", "carne asada", "pollo", "pescado"] #ARRAY

 for zebra in tacos # la palabra zebra es una VARIABLE, es como queres que se llame la variable, nombre arbitrario que le doy a CADA VARIABLE del array
    puts "Eat #{zebra} tacos!"
 end

 # its gonna do this for every variable in the array
